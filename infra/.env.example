# --- Postgres ---
POSTGRES_HOST=10.192.4.50
POSTGRES_PORT=5432
POSTGRES_USER=ocr
POSTGRES_PASSWORD=ocr
POSTGRES_DB=ocr
# Hoặc set trực tiếp (ưu tiên hơn POSTGRES_*):
# DATABASE_URL=postgresql+psycopg://user:pass@host:5432/dbname

# --- Redis (Celery broker) ---
CELERY_BROKER_URL=redis://10.192.4.50:6379/0
CELERY_RESULT_BACKEND=redis://10.192.4.50:6379/0

# --- MinIO (copy thành .env trong infra/) ---
MINIO_ENDPOINT=10.192.4.50:9002
MINIO_ACCESS_KEY=minioadmin
MINIO_SECRET_KEY=minioadmin123
MINIO_SECURE=false
MINIO_OCR_BUCKET=ocr

# --- OCR pipeline (optional; CRAFT/VietOCR từ infra/system_config.yml nếu set) ---
# OCR_DEVICE=cpu
OCR_SYSTEM_CONFIG=/path/to/infra/system_config.yml
# OCR_CONFIG_BASE=/path/to/repo   # base để resolve path trong yml (models/...)
# OCR_MAX_SIDE=1200
# VIETOCR_WEIGHTS=/path/to/vietocr_weights.pth
# CRAFT_REFINER=true
# CRAFT_WEIGHTS_CRAFT_NET=  # fallback nếu không có system_config
# CRAFT_WEIGHTS_REFINE_NET=
